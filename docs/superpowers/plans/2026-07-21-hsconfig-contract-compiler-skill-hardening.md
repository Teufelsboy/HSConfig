# HSConfig Contract Compiler Skill Hardening Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the HSConfig Codex skill slimmer and harder to misuse by routing every config-generation session through one compact contract-compiler checklist without changing runtime generation behavior.

**Architecture:** Add one canonical skill reference file that summarizes currentness, source, contract, runtime, quality, and operator gates. Link it from the skill entrypoint and workflow reference, then guard it with focused tests and the existing contract guardrail runner. This is a skill/documentation-as-code hardening only: no new runtime surfaces, no gameplay engine, no HSTuner coupling, and no source-status promotion shortcut.

**Tech Stack:** Markdown, Python 3.11, pytest, existing HSConfig scripts. No new dependencies.

## Global Constraints

- Work only in `C:\Users\darbo\Documents\HSConfig`.
- Start by refreshing repository state:
  ```powershell
  cd C:\Users\darbo\Documents\HSConfig
  git fetch --all --prune --tags
  python scripts\check_hsconfig_currentness.py --cwd . --json
  git status --short --branch
  git log -1 --oneline
  ```
- Required pre-edit state: `dirty=false`, `clean_for_runtime_work=true`, and `behind_origin_main=0`.
- Preserve `reports/operator_summary.json` as the only normal apply authority.
- Preserve `SOURCE_BACKED_STRONG` as an evidence-quality label, not a generation or apply gate.
- Preserve the universal no-block rule: source-depth warnings, weak guide coverage, report-only mechanics, unsupported claims, and default-only visibility are diagnostics, not hard blockers for technically valid load-safe packages.
- Preserve no-silent-default-only visibility: default-only surfaces must be reported, not hidden or treated as source-backed.
- Preserve the normal runtime output boundary: `GlobalValues.json`, `Mulligan.json`, per-card `<CARDID>.json`, and `Combo.json` only when exact ordered combo evidence exists.
- Do not introduce `Presume.json`, `Concede.json`, aggregate `CardBehavior.json`, replay parsing, winrate analysis, runtime log analysis, HSTuner behavior, or gameplay sequencing logic.
- Preserve the Darkbishop Benedictus boundary: `SW_448` can retain start-of-game and hero-power-transform semantics, but must not become a Mulligan keep without explicit opening-hand source text.
- Keep the installed skill synchronized with `scripts\sync_installed_skill.py`.
- If implementation changes pass verification, commit them so the final worktree is clean.

---

## File Structure

- Create: `tests/test_skill_contract_entrypoint.py`
  - Responsibility: focused tests for the compact contract-compiler checklist, entrypoint links, and no runtime-surface expansion language.
- Create: `.agents/skills/hsconfig/references/contract-compiler-checklist.md`
  - Responsibility: the concise skill-owned checklist agents must follow before generated config handoff or runtime-facing apply review.
- Modify: `.agents/skills/hsconfig/SKILL.md`
  - Responsibility: link to the checklist from the normal workflow without repeating the full contract.
- Modify: `.agents/skills/hsconfig/references/workflow.md`
  - Responsibility: link the workflow reference back to the same checklist so staged and one-command paths use one contract.
- Modify: `tests/test_skill_files.py`
  - Responsibility: include the new reference file in the required skill package and assert the entrypoint stays compact.
- Modify: `scripts/check_contract_guardrails.py`
  - Responsibility: run the new contract-entrypoint tests in the existing guardrail command.
- Modify: `tests/test_check_contract_guardrails.py`
  - Responsibility: prove the guardrail runner includes the new test file.
- Generated by sync: `C:\Users\darbo\.codex\skills\hsconfig\references\contract-compiler-checklist.md`
  - Responsibility: installed copy of the repo skill reference, created by `python scripts\sync_installed_skill.py`.

---

### Task 1: Add Failing Contract Entrypoint Tests

**Files:**
- Create: `tests/test_skill_contract_entrypoint.py`

**Interfaces:**
- Consumes: `.agents/skills/hsconfig/SKILL.md`
- Consumes: `.agents/skills/hsconfig/references/workflow.md`
- Consumes: `.agents/skills/hsconfig/references/contract-compiler-checklist.md`
- Produces: three focused pytest tests used by `scripts/check_contract_guardrails.py`

- [ ] **Step 1: Create the test file with exact expectations**

  ```python
  from pathlib import Path


  ROOT = Path(__file__).resolve().parents[1]
  SKILL_ROOT = ROOT / ".agents" / "skills" / "hsconfig"
  CHECKLIST = SKILL_ROOT / "references" / "contract-compiler-checklist.md"
  ENTRYPOINT_LINK = "Contract compiler checklist: `references/contract-compiler-checklist.md`."

  REQUIRED_CHECKLIST_LINES = [
      "1. Currentness gate: run `git fetch --all --prune --tags`, `python scripts/check_hsconfig_currentness.py --cwd . --json`, and `git status --short --branch` before runtime-facing work.",
      "2. Source gate: candidate registries and `source_closure_intake_receipt.json` are acquisition input only; they cannot promote, block, write runtime config, or replace `reports/operator_summary.json`.",
      "3. Claim gate: source text must normalize to an explicit `claim_kind`; effect relevance, guide importance, and archetype value do not bypass claim-kind or surface gates.",
      "4. Runtime gate: normal output remains `GlobalValues.json`, `Mulligan.json`, per-card `<CARDID>.json`, and `Combo.json` only for exact ordered combo evidence.",
      "5. Quality gate: no silent default-only runtime success; every expected surface is emitted, explicitly suppressed, or reported as a visible source/action gap.",
      "6. Strong gate: `SOURCE_BACKED_STRONG` requires honest source closure and `default_only_runtime_surfaces=[]`; it is not needed for load-safe apply.",
      "7. No-block gate: source warnings, warning-only mechanics, unresolved options, and thin guide coverage do not block a technically valid load-safe package.",
      "8. Operator gate: open `reports/operator_summary.json` first; it remains the only normal apply authority.",
      "9. Darkbishop gate: preserve `SW_448` hero-power-transform semantics, but do not emit a Mulligan keep without explicit opening-hand source text.",
      "10. Boundary gate: do not add `Presume.json`, `Concede.json`, aggregate `CardBehavior.json`, replay parsing, winrate analysis, HSTuner tuning, or gameplay sequencing logic.",
  ]

  FORBIDDEN_CHECKLIST_PHRASES = [
      "source report apply authority",
      "candidate URL proves strong",
      "default-only strong",
      "normal HSConfig output includes `Presume.json`",
      "normal HSConfig output includes `Concede.json`",
      "parse runtime logs to tune values",
      "HSTuner fallback",
  ]


  def test_skill_and_workflow_link_contract_compiler_checklist():
      skill = (SKILL_ROOT / "SKILL.md").read_text(encoding="utf-8")
      workflow = (SKILL_ROOT / "references" / "workflow.md").read_text(
          encoding="utf-8"
      )

      assert CHECKLIST.exists()
      assert ENTRYPOINT_LINK in skill
      assert ENTRYPOINT_LINK in workflow
      assert skill.count(ENTRYPOINT_LINK) == 1
      assert workflow.count(ENTRYPOINT_LINK) == 1


  def test_contract_compiler_checklist_owns_canonical_runtime_boundaries():
      text = CHECKLIST.read_text(encoding="utf-8")

      assert text.count("# Contract Compiler Checklist") == 1
      for line in REQUIRED_CHECKLIST_LINES:
          assert line in text
      assert "`operator_summary.json` remains the only normal apply authority." in text
      assert "`source_contract_audit.json` is diagnostic." in text
      assert "`source_to_runtime_explainability.json` is diagnostic." in text
      assert "`source_evidence_closure.json` is diagnostic." in text


  def test_contract_compiler_checklist_does_not_expand_runtime_scope():
      text = CHECKLIST.read_text(encoding="utf-8")
      lowered = text.lower()

      for phrase in FORBIDDEN_CHECKLIST_PHRASES:
          assert phrase.lower() not in lowered
      assert "does not parse replays" in lowered
      assert "does not inspect winrate" in lowered
      assert "does not analyze runtime logs" in lowered
      assert "does not tune after games" in lowered
  ```

- [ ] **Step 2: Run the new test and verify it fails before implementation**

  ```powershell
  cd C:\Users\darbo\Documents\HSConfig
  python -m pytest tests\test_skill_contract_entrypoint.py -q -p no:cacheprovider
  ```

  Expected failure:

  ```text
  AssertionError
  ```

  The first expected cause is that `.agents/skills/hsconfig/references/contract-compiler-checklist.md` does not exist yet.

---

### Task 2: Add The Contract Compiler Checklist Reference

**Files:**
- Create: `.agents/skills/hsconfig/references/contract-compiler-checklist.md`

**Interfaces:**
- Consumed by: `.agents/skills/hsconfig/SKILL.md`
- Consumed by: `.agents/skills/hsconfig/references/workflow.md`
- Verified by: `tests/test_skill_contract_entrypoint.py`

- [ ] **Step 1: Create the reference file with this exact content**

  ```markdown
  # Contract Compiler Checklist

  Use this checklist before generated config handoff, source-depth work, or runtime-facing apply review. It is not another operator gate; it is the compact pre-run contract the skill follows before reading detailed references.

  1. Currentness gate: run `git fetch --all --prune --tags`, `python scripts/check_hsconfig_currentness.py --cwd . --json`, and `git status --short --branch` before runtime-facing work.
  2. Source gate: candidate registries and `source_closure_intake_receipt.json` are acquisition input only; they cannot promote, block, write runtime config, or replace `reports/operator_summary.json`.
  3. Claim gate: source text must normalize to an explicit `claim_kind`; effect relevance, guide importance, and archetype value do not bypass claim-kind or surface gates.
  4. Runtime gate: normal output remains `GlobalValues.json`, `Mulligan.json`, per-card `<CARDID>.json`, and `Combo.json` only for exact ordered combo evidence.
  5. Quality gate: no silent default-only runtime success; every expected surface is emitted, explicitly suppressed, or reported as a visible source/action gap.
  6. Strong gate: `SOURCE_BACKED_STRONG` requires honest source closure and `default_only_runtime_surfaces=[]`; it is not needed for load-safe apply.
  7. No-block gate: source warnings, warning-only mechanics, unresolved options, and thin guide coverage do not block a technically valid load-safe package.
  8. Operator gate: open `reports/operator_summary.json` first; it remains the only normal apply authority.
  9. Darkbishop gate: preserve `SW_448` hero-power-transform semantics, but do not emit a Mulligan keep without explicit opening-hand source text.
  10. Boundary gate: do not add `Presume.json`, `Concede.json`, aggregate `CardBehavior.json`, replay parsing, winrate analysis, HSTuner tuning, or gameplay sequencing logic.

  ## Report Order

  1. `reports/operator_summary.json`
  2. `reports/source_to_runtime_explainability.json`
  3. `reports/source_evidence_closure.json`
  4. `reports/source_contract_audit.json`
  5. `reports/per_card_config_readiness_report.json`
  6. `reports/guide_source_depth_report.json`
  7. `reports/strong_promotion_report.json`
  8. `reports/card_behavior_plan_report.json`
  9. `reports/mulligan_plan_report.json`
  10. `reports/global_values_authority_matrix.json`

  `operator_summary.json` remains the only normal apply authority.
  `source_contract_audit.json` is diagnostic.
  `source_to_runtime_explainability.json` is diagnostic.
  `source_evidence_closure.json` is diagnostic.

  ## Non-Goals

  HSConfig does not parse replays.
  HSConfig does not inspect winrate.
  HSConfig does not analyze runtime logs.
  HSConfig does not tune after games.
  HSConfig does not replace HearthRanger gameplay decisions.
  ```

- [ ] **Step 2: Run the new tests again**

  ```powershell
  cd C:\Users\darbo\Documents\HSConfig
  python -m pytest tests\test_skill_contract_entrypoint.py -q -p no:cacheprovider
  ```

  Expected result after only this task: tests still fail because the entrypoint links are not wired yet.

---

### Task 3: Wire The Checklist Into The Skill And Workflow

**Files:**
- Modify: `.agents/skills/hsconfig/SKILL.md`
- Modify: `.agents/skills/hsconfig/references/workflow.md`
- Modify: `tests/test_skill_files.py`

**Interfaces:**
- Adds one exact link sentence:
  ```markdown
  Contract compiler checklist: `references/contract-compiler-checklist.md`.
  ```

- [ ] **Step 1: Add the link to `.agents/skills/hsconfig/SKILL.md`**

  Insert this line immediately after the existing `3. Open reports/operator_summary.json first.` normal workflow item:

  ```markdown
  Contract compiler checklist: `references/contract-compiler-checklist.md`.
  ```

  Keep the rest of the skill content intact unless an existing nearby sentence becomes an exact duplicate.

- [ ] **Step 2: Add the same link to `.agents/skills/hsconfig/references/workflow.md`**

  Insert this line immediately after the existing one-line normal workflow paragraph:

  ```markdown
  Contract compiler checklist: `references/contract-compiler-checklist.md`.
  ```

- [ ] **Step 3: Update `tests/test_skill_files.py` required files**

  Add this path to the `expected` set in `test_skill_has_required_files()`:

  ```python
  "references/contract-compiler-checklist.md",
  ```

- [ ] **Step 4: Add a compactness assertion in `tests/test_skill_files.py`**

  In `test_skill_and_workflow_stay_compact_and_canonical()`, add:

  ```python
  checklist = (SKILL_ROOT / "references" / "contract-compiler-checklist.md").read_text(
      encoding="utf-8"
  )
  assert "Contract compiler checklist: `references/contract-compiler-checklist.md`." in skill
  assert "Contract compiler checklist: `references/contract-compiler-checklist.md`." in workflow
  assert "reports/operator_summary.json" in checklist
  assert "SOURCE_BACKED_STRONG" in checklist
  assert "default_only_runtime_surfaces=[]" in checklist
  assert "Darkbishop gate" in checklist
  ```

- [ ] **Step 5: Run the skill tests**

  ```powershell
  cd C:\Users\darbo\Documents\HSConfig
  python -m pytest tests\test_skill_contract_entrypoint.py tests\test_skill_files.py -q -p no:cacheprovider
  ```

  Expected result:

  ```text
  passed
  ```

---

### Task 4: Register The New Tests In The Guardrail Runner

**Files:**
- Modify: `scripts/check_contract_guardrails.py`
- Modify: `tests/test_check_contract_guardrails.py`

**Interfaces:**
- Existing tuple: `FOCUSED_CONTRACT_TESTS`
- Add:
  ```python
  "tests/test_skill_contract_entrypoint.py",
  ```

- [ ] **Step 1: Add the new test file to `scripts/check_contract_guardrails.py`**

  Insert the new test near the existing skill tests:

  ```python
  "tests/test_skill_sync.py",
  "tests/test_skill_files.py",
  "tests/test_skill_contract_entrypoint.py",
  "tests/test_claim_kind_runtime_contract.py",
  ```

- [ ] **Step 2: Update `tests/test_check_contract_guardrails.py`**

  In `test_guardrail_runner_includes_contract_invariant_closure_tests()`, add:

  ```python
  "tests/test_skill_contract_entrypoint.py",
  ```

  In `test_guardrail_commands_include_skill_sync_sentinel_and_boundary_suite()`, add:

  ```python
  assert "tests/test_skill_contract_entrypoint.py" in commands[2].argv
  ```

- [ ] **Step 3: Run guardrail runner tests**

  ```powershell
  cd C:\Users\darbo\Documents\HSConfig
  python -m pytest tests\test_check_contract_guardrails.py -q -p no:cacheprovider
  ```

  Expected result:

  ```text
  passed
  ```

---

### Task 5: Sync Installed Skill And Verify Contract Guardrails

**Files:**
- Generated by sync: `C:\Users\darbo\.codex\skills\hsconfig\references\contract-compiler-checklist.md`

**Interfaces:**
- Existing command:
  ```powershell
  python scripts\sync_installed_skill.py
  ```
- Existing check:
  ```powershell
  python scripts\sync_installed_skill.py --check
  ```

- [ ] **Step 1: Sync the installed skill**

  ```powershell
  cd C:\Users\darbo\Documents\HSConfig
  python scripts\sync_installed_skill.py
  ```

  Expected output contains:

  ```text
  Synced HSConfig skill to
  ```

- [ ] **Step 2: Verify installed-skill sync**

  ```powershell
  cd C:\Users\darbo\Documents\HSConfig
  python scripts\sync_installed_skill.py --check
  ```

  Expected output contains:

  ```text
  HSConfig skill is in sync
  ```

- [ ] **Step 3: Run the focused skill and guardrail tests**

  ```powershell
  cd C:\Users\darbo\Documents\HSConfig
  python -m pytest tests\test_skill_contract_entrypoint.py tests\test_skill_files.py tests\test_skill_sync.py tests\test_check_contract_guardrails.py -q -p no:cacheprovider
  python scripts\check_contract_guardrails.py
  ```

  Expected result:

  ```text
  OK: installed skill sync
  OK: contract spine sentinel
  OK: focused contract boundary tests
  ```

---

### Task 6: Final Verification And Clean Commit

**Files:**
- All changed files from Tasks 1 through 5.

**Interfaces:**
- Verification commands only.

- [ ] **Step 1: Run the full test suite**

  ```powershell
  cd C:\Users\darbo\Documents\HSConfig
  python -m pytest -q -p no:cacheprovider
  ```

  Expected result:

  ```text
  passed
  ```

- [ ] **Step 2: Check formatting and generated artifacts**

  ```powershell
  cd C:\Users\darbo\Documents\HSConfig
  git diff --check
  git status --short
  ```

  Expected repository changes before commit:

  ```text
  A .agents/skills/hsconfig/references/contract-compiler-checklist.md
  M .agents/skills/hsconfig/SKILL.md
  M .agents/skills/hsconfig/references/workflow.md
  M scripts/check_contract_guardrails.py
  M tests/test_check_contract_guardrails.py
  M tests/test_skill_files.py
  A tests/test_skill_contract_entrypoint.py
  ```

  Expected installed skill sync changes outside the repo:

  ```text
  A C:\Users\darbo\.codex\skills\hsconfig\references\contract-compiler-checklist.md
  M C:\Users\darbo\.codex\skills\hsconfig\SKILL.md
  M C:\Users\darbo\.codex\skills\hsconfig\references\workflow.md
  ```

  These installed skill paths are outside the repo and will not be staged by repo Git, but must be synced and clean according to `scripts\sync_installed_skill.py --check`.

- [ ] **Step 3: Remove generated test artifacts if present**

  ```powershell
  cd C:\Users\darbo\Documents\HSConfig
  Remove-Item -LiteralPath .pytest_cache -Recurse -Force -ErrorAction SilentlyContinue
  Remove-Item -LiteralPath outputs\verify-contract-compiler-checklist -Recurse -Force -ErrorAction SilentlyContinue
  ```

- [ ] **Step 4: Commit only repository changes**

  ```powershell
  cd C:\Users\darbo\Documents\HSConfig
  git add .agents\skills\hsconfig\SKILL.md .agents\skills\hsconfig\references\workflow.md .agents\skills\hsconfig\references\contract-compiler-checklist.md scripts\check_contract_guardrails.py tests\test_check_contract_guardrails.py tests\test_skill_files.py tests\test_skill_contract_entrypoint.py
  git commit -m "docs: harden hsconfig contract compiler skill"
  git status --short --branch
  python scripts\check_hsconfig_currentness.py --cwd . --json
  ```

  Expected final repository state:

  ```text
  no uncommitted repository changes
  dirty=false
  clean_for_runtime_work=true
  behind_origin_main=0
  ```

---

## Subagent Split

- **Explorer Agent:** read-only pass over `.agents/skills/hsconfig/SKILL.md`, `.agents/skills/hsconfig/references/workflow.md`, `tests/test_skill_files.py`, and `scripts/check_contract_guardrails.py`; confirm exact insertion points and existing sentinel wording.
- **Test Agent:** create `tests/test_skill_contract_entrypoint.py`, then stop after confirming the new test fails for the missing checklist/link.
- **Docs Agent:** create `.agents/skills/hsconfig/references/contract-compiler-checklist.md` and add the two link sentences in the skill and workflow files.
- **Guardrail Agent:** update `scripts/check_contract_guardrails.py`, `tests/test_check_contract_guardrails.py`, and `tests/test_skill_files.py`.
- **Main Agent:** sync the installed skill, run all verification, remove generated artifacts, commit repo changes, and confirm clean/current state.

No two agents should write the same file. The main agent owns final conflict resolution, installed-skill sync, and verification.

---

## Acceptance Criteria

- The HSConfig skill points to one compact `contract-compiler-checklist.md`.
- The workflow reference points to the same checklist.
- The checklist states currentness, source, claim, runtime, quality, strong, no-block, operator, Darkbishop, and boundary gates.
- The checklist explicitly keeps replay parsing, winrate analysis, runtime log analysis, HSTuner tuning, and gameplay sequencing out of HSConfig.
- The checklist does not introduce another operator gate, apply authority, runtime surface, or SOURCE_BACKED_STRONG shortcut.
- The installed skill is synced with the repo skill.
- `python scripts\check_contract_guardrails.py` passes.
- `python -m pytest -q -p no:cacheprovider` passes.
- The final worktree has no uncommitted changes.
