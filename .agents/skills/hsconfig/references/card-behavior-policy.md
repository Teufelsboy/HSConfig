# Card Behavior Policy

Every deck card must be represented in the gameplan contract.

Emit `<CARDID>.json` when documented VisionAI syntax can express a guide-backed behavior, priority, target, discover, choice, attack, hero-power, or timing rule. If a claim cannot be lowered safely, keep it in reports instead of inventing unsupported runtime syntax.
